To create a new table in an existing database, use the command:; NEW TABLE, CREATE TABLE, MAKE TABLE;
The query for selecting all values from the "Persons" table has the form:; SELECT ALL Persons, SELECT * FROM Persons, SELECT .[Persons];
The query for selecting the first 14 records from the "Users" table has the form:; SELECT * FROM Users LIMIT 14, SELECT * LIMIT 14 FROM Users, SELECT * FROM USERS;
Write a query that will return the current date:; SELECT GetDate(), SELECT TodayDate(), SELECT Date(Today);
Which operator is used to select values within a given range?; WITHIN, IN, BETWEEN;